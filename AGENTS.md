# 风之岚 AI 服务统一入口 - 需求拆解文档

## 产品概述

- **产品类型**: 品牌官网入口页 / 身份分流落地页（Landing Portal）
- **场景类型**: <scene_type>prototype-app</scene_type>
- **目标用户**: 首次到访风之岚科技的三类潜在客户——①个人创业者、独立顾问、自由职业者、小型工作室负责人、1～5 人团队；②企业经营者、部门负责人、数字化/信息化负责人、HR/培训负责人、部门试点发起人；③政府与国企项目发起或采购人员，园区、协会、商会、高校、企业服务机构等组织方
- **核心价值**: 用户进入后 10 秒内"对号入座"识别自己的身份定位，点击对应卡片进入该人群的二级服务官网
- **界面语言**: 中文（zh-CN）
- **主题偏好**: 浅色（light）——沿用已上线 solo 页的品牌视觉：橙 / 黑 / 白高对比配色、粗黑标题、硬投影、新粗野主义（Neo-Brutalism）卡片风格
- **导航模式**: 锚点导航（单页两屏，极简）
- **导航布局**: Topbar（仅品牌 + 1 个锚点 + 品牌承诺短语，不设复杂菜单）
- **指定组件**: **Spline 3D 场景嵌入组件**（用户 @Spline 3D 明确指定，必须复用当前应用中已有的 3D 场景，作为首屏视觉核心/背景）

---

## 背景事实（来自预研，下游必须知晓）

1. **三个外链目标的真实状态**（2026-09-12 实测）：
   - `https://fengzhilanai.github.io/solo/` ✅ 已上线（个人创业者与小团队服务页，橙色品牌风，含导航/Hero/服务定价/FAQ 等长页面，咨询表单为飞书多维表格表单 `https://qcnrt01y72yf.feishu.cn/share/base/form/shrcn53nHKog9UxwnaIaE82swZe`）。
   - `https://fengzhilanai.github.io/biz/` ⚠️ 当前返回 **404**（"这条路还没有建好。"，页面尚未发布）。
   - `https://fengzhilanai.github.io/org/` ⚠️ 当前返回 **404**（同上）。
   - **处理原则**：按用户明确要求，入口页仍须原样配置这三个跳转链接（biz/org 为预留入口，二级页后续建成即自动生效），**不得**因当前 404 而删改链接或替换为其他地址。
2. **品牌资产事实**：品牌名"风之岚科技 / FENGZHILAN · AI TECHNOLOGY"，Logo 为"岚"字图形标；solo 页主色为亮橙 + 纯黑 + 米白，硬边框 + 硬阴影卡片，按钮为纯黑块面。新入口页视觉须与该体系一致，使三个站点看起来是同一品牌族。
3. **历史调研附件**：本轮对话含 3 张目标站截图（`screenshot-1789187647886.png` 为 solo 整页长截图；另两张为 biz/org 的 404 截图），供 design-agent 阶段取色与风格对齐参考。
4. 用户明确要求"**1 到 2 屏**"，严格控制篇幅：不做长落地页、不放案例/FAQ/定价/页脚链接矩阵等发散内容。

---

## 页面结构总览

> 单页应用，仅 1 个页面、2 个滚动屏次。三张身份卡片在两屏中均承担分流职责（首屏卡片可只露标题与"适合谁"，第二屏看全服务明细）。

**页面文件**: `HomePage.tsx`

| 区块名称 | 锚点 | 屏次 | 区块说明 |
|---------|------|------|---------|
| 极简顶栏 | — | 第 1 屏顶部 | 左侧"风之岚科技"品牌标识，右侧 1 个锚点链接"三条路径"，无 CTA 按钮堆叠 |
| 首屏身份分流 Hero | `#hero`（页顶） | 第 1 屏（占满一屏） | 大标题身份引导 + Spline 3D 场景（视觉核心/背景）+ 三张身份入口大卡片（solo / biz / org），整卡可点击外链跳转；底部下滚提示 |
| 三条路径明细 | `#paths` | 第 2 屏 | 三列并排（移动端纵向堆叠）：每列含编号、身份名、一句核心主张、"适合谁"标签行、服务清单（带一句话注解）、弹性阶段说明（仅 biz/org 列）、底部"进入专属页面 ↗"外链按钮 |
| 极简页脚 | — | 第 2 屏底部 | 一行：© 风之岚科技 FENGZHILAN · AI TECHNOLOGY（不做链接矩阵） |

**一致性校验**：
- 导航 → 页面：顶栏唯一锚点"三条路径"→ `#paths` 区块存在，无死链。✅
- 页面 → 入口：单页即全部内容，无孤岛区块。✅
- 三个外链分别出现在首屏卡片与第二屏按钮，共 6 个跳转点，URL 全部来自用户原话。✅

---

## 页面布局建议

- **布局模式**: 第 1 屏采用**左右/层叠分区**——Spline 3D 画布作为全屏背景或右侧视觉主体（桌面端建议 3D 居右或居中后置，文字与卡片压在其上，保证文字对比度与可读性）；三张身份卡片横向一排悬浮于 3D 场景下层之上。第 2 屏采用**三等分列**常规文档流。
- **视觉重心**: 第 1 屏重心在"**身份选择**"——三张卡片必须是最醒目的可点元素，3D 效果负责营造科技感与品牌调性，不得喧宾夺主导致卡片不可读；第 2 屏重心在"**服务内容对照**"。
- **结果承载区**: 本页不产生数据结果，"结果"即用户的分流决策——承载区就是三张入口卡片及其第二屏明细列；初始态直接完整呈现（无空状态/无加载骨架需求，Spline 场景加载期除外）。
- **响应式**: 桌面端三卡横排；平板两排；移动端单列纵向堆叠，3D 场景降级为卡片上方的矮幅场景区或静态首帧图，确保首屏标题+第一张卡片不被挤压。
- **3D 性能与兜底**: Spline 加载中显示品牌色占位/Loading；加载失败显示纯色/渐变降级背景，卡片与文案不受影响；移动端允许降低画质或暂停逐帧渲染。

---

## Spline 3D 组件整合规划（用户指定组件，必做）

| 整合位置 | 用法 | 交互要求 |
|---------|------|---------|
| 第 1 屏 Hero | 复用**当前应用中已有的 Spline 3D 场景**（用户原话"基于当前的 3D 效果"），以 Spline 3D 场景嵌入组件承载，作为首屏背景/主视觉 | ① 场景可随鼠标轻微视差旋转；② 三张身份卡片 hover 时，场景可做轻微聚焦/色调响应（**增强项，非 P0**，若组件事件对接成本高则省略，保证视觉与跳转优先）；③ 3D 画布不遮挡卡片点击（pointer-events 分层处理） |
| 第 2 屏 | 不嵌入第二个场景，避免重复与性能负担 | — |

> 场景地址/资源由 design-agent 从当前应用已有的 Spline 3D 组件配置中继承，本期不新建、不替换 3D 场景。

---

## 导航配置

- **导航布局**: Topbar（顶部固定、极简、半透明底以露出 3D 场景）
- **导航项**:

| 导航文字 | 锚点 |
|---------|------|
| 三条路径 | `#paths` |

> 顶栏品牌名点击回到 `#hero` 页顶。外链落地页不设"首页/关于/联系"等未实现入口。

---

## 数据来源声明

| 数据/操作 | 来源类型 | 实现要求 | mock 兜底 |
|---|---|---|---|
| 三类身份与服务内容（标题、核心主张、适合人群、服务清单及注解、阶段说明、外链 URL） | demo-mock | 在 `src/data/paths.ts` 以类型化常量数组导出（建议 3 条记录，字段：id / title / tagline / audiences[] / services[]{name,desc} / footnote? / url / accent），页面通过 map 渲染，文案**必须逐字使用用户原话**，不得自行改写或增删服务项 | ✅ 本身即静态展示内容（唯一真实数据就是用户提供的文案与 URL） |
| 跳转三个二级官网 | demo-mock（静态外链） | 外链 URL 存放于 `paths.ts` 的 `url` 字段，渲染为普通 `<a href>`；在**当前窗口**打开（属于主流程导航，非外链备案场景）；链接须保留 `https://fengzhilanai.github.io/solo/`、`/biz/`、`/org/` 原样，biz/org 虽暂为 404 也不得改动 | 无（URL 为用户指定真实地址，禁止替换为 # 或占位链接） |
| Spline 3D 场景展示 | 组件能力（非六类数据需求） | 使用用户指定的 Spline 3D 场景嵌入组件，复用当前应用已有场景配置；加载中/失败提供品牌色降级背景 | 加载失败时降级为静态品牌背景，不影响文案与跳转 |

> 本页**无 AI/飞书插件需求**（纯品牌分流展示页，无生成、识别、解析、表单提交等能力诉求；咨询表单留在各二级页，本页不承接），故不含「插件规划」章节。

---

## 三类身份内容数据（paths.ts 文案底稿，逐字源自用户原话）

### 01 个人创业者与小团队
- **URL**: `https://fengzhilanai.github.io/solo/`
- **核心主张**: 先做一个可以实际试用、也方便后续接手的第一版。
- **适合谁**: 个人创业者 / 独立顾问 / 自由职业者 / 小型工作室负责人 / 1～5 人团队
- **服务**:
  - AI 项目诊断：还不确定先解决什么问题
  - AI 项目共创：双方共同做出第一版
  - AI 定制实现：由工作室主导设计、实现与交接
  - AI 实战训练营：学习方法并亲手完成一个小成果

### 02 中小企业与部门团队
- **URL**: `https://fengzhilanai.github.io/biz/`
- **核心主张**: 先验证一个真实的部门场景，再决定是否推广。
- **适合谁**: 企业经营者 / 部门负责人 / 数字化或信息化负责人 / HR/培训负责人 / 部门试点发起人
- **服务**:
  - 部门 AI 机会盘点：形成场景优先级和首个试点建议
  - 单部门 AI 可控试点：在有限范围内验证一个真实业务问题
  - AI 运行优化：根据使用记录持续改善质量、异常、规则、知识和成本
- **阶段说明（列底注脚）**: 每个阶段都可以独立结束，也可以根据真实试用结果继续、调整或推广。

### 03 政企、园区、协会与高校组织方
- **URL**: `https://fengzhilanai.github.io/org/`
- **核心主张**: 把 AI 项目变成可采购、可实施、可验收的成果。
- **适合谁**: 政府与国企项目发起或采购人员 / 园区 / 协会 / 商会 / 高校 / 企业服务机构和其他组织方
- **服务**:
  - AI 专题共识会：围绕明确议题建立共同认知并形成问题清单
  - 场景梳理与试点方案：形成场景优先级、责任角色和可立项范围
  - 岗位 AI 任务实训：围绕岗位任务形成练习、规范和应用计划
  - 单场景可控试点：完成有限范围的试运行、验收、交接和下一步建议
- **阶段说明（列底注脚）**: 活动、方案、实训和技术试点可以独立采购，不机械包装成一个大项目。

---

## 功能列表

- **区块: 极简顶栏**
  - **页面目标**: 品牌识别 + 提供唯一的内容锚点
  - **功能点**:
    - 品牌标识展示与回顶：点击"风之岚科技"平滑滚动回 `#hero`
    - 锚点跳转：点击"三条路径"平滑滚动至 `#paths`，吸顶态与 3D 背景叠加时保证可读性

- **区块: 首屏身份分流 Hero（`#hero`）**
  - **页面目标**: 用户 10 秒内完成身份识别并产生点击
  - **功能点**:
    - 身份引导文案：大标题（建议方向"你是谁？从这里进入适合你的 AI 路径"）+ 一句副标，降低选择成本
    - **Spline 3D 场景嵌入**：复用当前已有 3D 效果作为首屏主视觉，支持鼠标视差；含加载中占位与失败降级背景
    - 三张身份入口大卡片：编号 + 身份名 + 核心主张 + 适合人群标签速览；hover 态强反馈（描边变色/位移/硬投影增强，呼应品牌硬投影风格）
    - 整卡跳转：点击卡片任意位置在当前窗口跳转对应 `paths.ts` 中的二级官网 URL；移动端整张卡可点，不依赖小按钮
    - 下滚引导：底部滚动提示引导至第 2 屏明细

- **区块: 三条路径明细（`#paths`）**
  - **页面目标**: 帮犹豫的用户通过服务清单完成二次确认
  - **功能点**:
    - 三列服务明细渲染：由 `src/data/paths.ts` 数据驱动 map 渲染三列，包含适合人群标签、服务名+一句话注解、biz/org 的阶段弹性注脚
    - 列 CTA 跳转：每列底部"进入专属页面 ↗"按钮跳转对应外链；按钮视觉三列可做差异化色阶但同属橙黑体系
    - 列 hover/聚焦高亮：当前查看列提升对比，辅助横向比较
    - 响应式重排：桌面三列 → 平板两列+单列 → 移动单列堆叠，外链按钮在移动端固定为列底部大按钮

- **区块: 极简页脚**
  - **页面目标**: 品牌收尾
  - **功能点**:
    - 单行品牌署名展示（纯静态，无功能跳转）

---

## 数据共享配置

无。单页静态展示，无跨页数据、无用户输入、无持久化需求。

---

## 实现边界（防止发散）

- **只做 1 个页面、2 个屏次**，不新增"关于我们/案例/定价/FAQ/联系"等任何区块或页面。
- 不做登录注册、不做主题切换、不做咨询表单（咨询转化在二级页完成）。
- 不新建/不更换 Spline 3D 场景，只复用当前 3D 效果。
- 三个外链 URL 原样使用；biz、org 当前 404 属预期（预留入口），不做特殊拦截弹窗。
- 文案以用户原话为唯一真相源，服务项数量与措辞不得增删改写。

-------

<scene_type>prototype-app</scene_type>

# UI 设计指南

## 1. 设计推导依据

- **参考意图**: Exact Reference —— 已上线的 `/solo/` 是完成度很高的品牌成品，`/biz/`、`/org/` 的 404 页也共用同一套橙色/墨黑 token；本页是三个二级官网的统一入口，视觉必须与它们同源，不另起炉灶。
- **核心情绪 / 应用类型**: Landing 分流门——访客在 10 秒内产生「这就是我」的认领感，并带着信任点击进入对应站点；情绪是果断、踏实、有行动力，而非炫技。
- **独特记忆点**: 「三扇编号黑门」——橙色场域中悬浮一块墨黑硬阴影展台承载 Spline 3D，下方三张 01/02/03 编号粗黑边框身份卡，hover 时下压、阴影加重，像拉开三扇不同的门。

## 2. Art Direction

- **方向名**: 橙色粗野主义门户（Orange Brutalism Gateway）
- **Design Style**: Neo Brutalism 新野兽派 + Swiss Editorial —— 与二级站的 2px 黑边、硬偏移阴影、橙色编号圆章、宽扁大写拉丁字完全同一语系；3D 只作为品牌场域中的「展品」，不改变语法。
- **DNA 参数**: 圆角 sharp（`rounded-none`，仅标签可 `rounded-sm`）/ 硬阴影 `4px 4px 0 墨黑`，hover 变 `7px 7px 0` 并位移 -2px / 间距 standard-spacious，8px 倍数 / 字体方向为重 grotesque + 思源黑体粗体 / 装饰为编号圆章、粗黑分隔线、橙色实色块、跑马灯条。
- **应用类型**: Landing —— 单屏主张 + 三列分流卡，信息密度低、行动路径极短。

## 3. Color System

**色彩关系**: 品牌橙主色 + 暖纸中性底 + 墨黑边框与文字；橙色只出现在色块、编号章、主 CTA 与 3D 展品材质上。
**配色设计理由**: 暖纸底比纯白更贴合「工作室手工做第一版」的踏实感，长段落不刺眼；墨黑承担 100% 边框与标题，保证粗野主义的力量骨架；主 CTA 为橙底黑字（对比约 5.2:1），次级按钮为白底黑边，不与橙色抢戏。
**主色推导**: 橙色直接抽取自 `/solo/` 首屏与 404 页的 `--orange`，是风之岚已建立的品牌识别；本页不创造新色相，只校准其在浅底上的明度。
**使用比例**: 60% 暖纸中性 / 30% 墨黑（边框、文字、3D 展台）/ 10% 品牌橙（CTA、编号、局部场域）。

| 角色 | CSS 变量 | Tailwind Class | HSL 值 | 设计说明 |
|---|---|---|---|---|
| bg | `--background` | `bg-background` | hsl(42 36% 96%) | 暖纸色页面底 |
| card | `--card` | `bg-card` | hsl(42 45% 99%) | 身份卡、弹层，比底略亮 |
| text | `--foreground` | `text-foreground` | hsl(30 18% 10%) | 墨黑标题正文 |
| textMuted | `--muted-foreground` | `text-muted-foreground` | hsl(30 8% 38%) | 标签、适用人群辅助说明 |
| primary | `--primary` | `bg-primary` / `text-primary` | hsl(24 96% 52%) | 品牌橙：CTA、编号章、hover 高亮 |
| primaryForeground | `--primary-foreground` | `text-primary-foreground` | hsl(30 25% 10%) | 橙底上的墨黑字标 |
| accent | `--accent` | `bg-accent` | hsl(24 85% 94%) | 浅橙反馈底：菜单 hover、选中行 |
| accentForeground | `--accent-foreground` | `text-accent-foreground` | hsl(24 60% 22%) | 浅橙底上的深棕文字 |
| border | `--border` | `border-border` | hsl(28 16% 13%) | 全站统一 2px 墨黑硬边 |

**语义色提示**: 仅用于服务承诺中的状态点（如「可独立结束 / 可继续推广」），全部降饱和以贴合暖纸色温：成功 bg hsl(152 28% 94%)、border hsl(155 42% 28%)、text hsl(155 60% 20%)；警告 bg hsl(40 95% 92%)、border hsl(38 92% 42%)、text hsl(32 90% 28%)（与橙主色同色系）；错误 bg hsl(354 72% 95%)、border hsl(354 62% 42%)、text hsl(354 70% 30%)。饱和度 ≤70%，不允许出现刺眼霓虹绿/红。

## 4. 字体与节奏

- **font-display**: Archivo Black / Archivo（拉丁）+ Noto Sans SC 900（中文）—— 复刻二级站宽粗大写拉丁与重磅中文标题的气质。
- **font-body**: Noto Sans SC 400/500/700 + Archivo —— 正文清晰，数字与英文小标签（`PICK YOUR PATH` 式）用 Archivo 大写加字距。
- **字号**: H1 text-5xl ~ text-7xl（可两排断行）；H2 text-2xl ~ text-3xl；卡片标题 text-xl；body text-base；muted/标签 text-sm。
- **圆角**: sharp —— 全站 `rounded-none`，与粗黑边框、硬阴影共同构成品牌手感。

## 5. 全局布局契约

- **Reference Layout Use**: 色彩、黑边宽度、硬阴影、编号橙章、橙色首屏场域全部取自 `/solo/` 参考；三卡分流结构为本页新推导。
- **Page / Section Order**: 极简导航 → Hero（左侧命题文案 + 右侧墨黑 Spline 展台）→ 三张身份卡（solo / biz / org，整卡外链）→ 底部承诺条 + 咨询入口；控制在桌面 1–2 屏内。
- **Standard Content Zone**: Landing `max-w-6xl` + `mx-auto`。
- **Shell / Frame Alignment**: 内容容器统一网格同宽对齐；Spline 展台与右侧卡片同列，不单独破网格。
- **Padding & Rhythm**: `px-4 md:px-8`，区块 `py-12 md:py-20`；卡片间 `gap-5 md:gap-6`。
- **Full-bleed Zones**: 仅 Hero 橙色场域与底部黑色承诺条 `w-full` 出血；内部文字、按钮、展台仍收在 max-w-6xl。
- **Local Narrowing**: Hero 文案列 `max-w-xl`；底部承诺语 `max-w-3xl` 居中。
- **Overflow Strategy**: 移动端三卡纵向堆叠，不使用横向滑动；跑马灯条用 `overflow-hidden` + 平移动画。
- **Flexibility Boundary**: 允许移动端内边距与 3D 高度自适应；max-w、2px 黑边、硬阴影、橙色与圆角语言全站锁定。

## 6. 视觉与动效

- **装饰**: 01/02/03 橙色实心圆章（白字/黑字 Archivo）、2px 墨黑分隔线、服务条目前的 `+` 记号、可选黑色跑马灯条（「诊断 · 共创 · 试点 · 实训」循环）。
- **阴影/边界**: 重而硬——卡片 `border-2 border-[hsl(28_16%_13%)] shadow-[5px_5px_0_#171310]`；Spline 展台为纯墨黑面板 hsl(28 20% 8%)，同样黑边硬阴影。
- **动效**: 克制造型、明确反馈——卡片 hover 整体 `-translate-x-0.5 -translate-y-0.5`、阴影扩到 7px，pressed 回落到 1px；入场元素 200ms 上浮；3D 保持低速自转并响应鼠标视差；`prefers-reduced-motion` 下冻结一切运动。

## 7. 组件原则

- 三张身份卡是同一组件的三个实例：编号、标题、人群标签、3–4 条服务要点（标题加粗 + 一句灰字说明）、底部「进入专属页面 ↗」行动行，整卡可点，外链 `target="_blank" rel="noopener"`。
- 按钮状态：Default 橙底黑边 / Hover 位移+阴影加深 / Active 回落 / Focus-visible 外加 2px 墨黑 offset 描边 / Disabled 降透明度 50% 且去阴影。
- Spline 组件：透明底导出、懒加载、加载中展示橙色斜纹占位骨架与「正在进入场域」文案；3D 为纯装饰时 `aria-hidden="true"`，信息不依赖它传达。
- 空/加载态延续暖纸 + 黑边语言，禁止回退到 shadcn 默认浅灰圆角卡。

## 8. Image Direction

- **Image Role**: 无栅格图片需求；唯一视觉主体是 Spline 3D 展品，充当品牌场域与「分流」隐喻。
- **Image Art Direction**: 单一主角式 3D 构成——一块或三块可联动的哑光实体（暗示三条路径：单体、网格、建筑群），材质为哑光橙、暖米白、墨黑塑料/烤漆，柔和工作室顶光，轻微接触阴影，形体在低速自转中偶尔重组；气质克制、可信、手工模型感，置于纯墨黑展台内，边缘不发光。
- **Image Prompt Keywords**: matte orange ceramic, warm off-white and ink black forms, three interlocking wayfinding objects, soft studio top light, subtle contact shadow, slow idle rotation, minimalist product render, brutalist toy model, neutral dark background
- **Image Avoidance**: 避免霓虹发光描边、紫色蓝渐变、赛博粒子、悬浮电路板/大脑/机器人套路图、金属铬反射、HUD 界面元素、多主体杂乱场景，以及任何高饱和彩虹材质。

## 9. Anti-patterns

- **Split personality**: 入口页用毛玻璃/紫渐变，点进 `/solo/` 变成橙黑粗野——首门与内室必须同一套 token、黑边与硬阴影。
- **3D takeover**: 让 Spline 撑满全屏、文字绕着 3D 跑、三张小卡被挤到首屏以下；分流任务永远优先于视觉表演。
- **Soft SaaS drift**: 卡片改成大圆角柔和投影、按钮变成默认蓝——一旦圆角变软，品牌识别即消失。
- **Orange overload**: 橙底 + 橙按钮 + 橙图标 + 橙链接同时出现；橙色只给 CTA、编号章和场域，其余交给墨黑与暖纸。
- **Phantom links**: biz/org 当前是 404，卡片仍要如实保留跳转但建议加「即将开放」小标记，避免访客以为出错；不得私自改链接去向。
- **Invisible interaction**: 3D 抢了 hover 焦点，卡片本身没有键盘 focus 态与按压反馈；每张卡必须有独立 `:focus-visible` 墨黑描边。
- **Status color drift**: 状态点使用高饱和翠绿/鲜红，在暖纸上刺眼；语义色必须降到与品牌橙一致的暖色系饱和度。